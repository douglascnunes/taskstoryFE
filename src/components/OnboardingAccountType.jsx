function OnboardingAccountType({ answers }) {

  // let results = [];
  // answers.forEach(element => {
  //   if (element[0] === 0) {
  //     results[0] += element[3]
  //   }
  //   else (
  //     results[1] += element[3]
  //   )
  // });
  // const result = results.map(elem => elem / 4)


  return (
    // <p>{result}</p>
    <p>ok</p>
  )
}

export default OnboardingAccountType;